services:
  frontend:
    environment:
      API_PROXY_TARGET: ${API_PROXY_TARGET:-http://api:8000}
    networks:
      - platform

networks:
  platform:
    name: ${PLATFORM_NETWORK:-platform-network}
    external: true
